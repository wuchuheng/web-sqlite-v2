import type { WhWasmInstallerContext } from "./installer-context.d.ts";

/**
 * Ensures the helper target exposes baseline metadata before installation proceeds.
 */
export declare function applyDefaults(context: WhWasmInstallerContext): void;
