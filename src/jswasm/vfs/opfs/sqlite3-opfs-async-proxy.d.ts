/**
 * Legacy OPFS async worker entry point that forwards to the refactored module implementation.
 *
 * This declaration ensures TypeScript consumers importing the historic worker path
 * receive a module-typed definition rather than falling back to `any`.
 */
export {};
